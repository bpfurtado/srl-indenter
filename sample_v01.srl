if(true) then {
    a=1;
    if(false) then {
        b=1;
        if(x=1) then {
            c=2;
        }
    }
};

if(foo=1) then {
    a=1;
    b=2;
    //if(foo=1) then {
    While(zeero) do {
        log(1);
        log(2);
        //Here {}
        log(3);
        for each integer in list do {
            if(
            it < 10
            and
            it > 0
            ) then {
                log("Found it");
            };
            //Old code }
        }
    }
}